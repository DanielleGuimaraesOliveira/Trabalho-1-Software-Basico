#include "bigint.c"
#include <stdio.h>

#define NUM_BITS 128
typedef unsigned char BigInt[NUM_BITS/8];

int testa_big_val (BigInt res, long val) {
    return 0;
}


int main (void) {
    printf("Alo");

    return 0;
}